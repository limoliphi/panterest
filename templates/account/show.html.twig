{% extends 'layouts/base.html.twig' %}

{% block title 'Profil' %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto text-center">
            <h1 class="my-5">Profile</h1>

            <img src="{{ app.user.gravatarurl }}" alt="{{ app.user.fullName}}'s avatar" class="shadow rounded-circle">

            <p class="mt-5 lead">{{ app.user.email }}</p>

            <hr>

            <h2>{{ app.user.fullName }}</h2>

            <p><small>🕒 Account created on {{ app.user.createdAt|date('d/m/Y') }}</small></p>

            <p>
                <a href="{{ path('app_account_edit') }}">Edit account</a> | <a href="#">Change password</a>
            </p>
        </div>
    </div>
{% endblock %}
